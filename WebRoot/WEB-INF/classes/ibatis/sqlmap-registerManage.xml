<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	<typeAlias alias="users" type="com.hgweibo.model.User"/>
	
	<insert id="addUser" parameterClass="users" >
		insert into users(userName,password,email,qq,nick,sex) 
			values(#userName#,#password#,#email#,#qq#,#nick#,#sex#)
	</insert>


	<delete id="deleteUser" parameterClass="users">
		delete from user
		where userName=#userName# 
	</delete>
	
	
	<select id="findUser" parameterClass="users" resultClass="users">
		SELECT *
		FROM users 
		WHERE userName = #userName# and password=#password#
	</select>
	

	<update id="updateUser" parameterClass="users">
	<![CDATA[
		update user set 
		    userName = #userName#,
		    password=#password#,
		    nick=#nick#,
		    trueName=#trueName#,
		    birthPlace=#birthPlace#,
		    sex=#sex#,
		    bloodType=#bloodType#,
		    email=#email#,
		    qq=#qq#,
		    education=#education#,
		    school=#school#,
		    major=#major#,
		    company=#company#,
		    job=#job#,
		    personalInstruction=#personalInstruction#,
		    emotion=#emotion#
		where usersId=#usersId#
	]]>
	</update>
 </sqlMap>